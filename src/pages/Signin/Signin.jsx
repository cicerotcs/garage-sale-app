import "./Signin.scss";
import AuthForm from "../../components/AuthForm/AuthForm";

const Signin = () => {
  return <AuthForm label="Sign in" action="signin" buttonLabel="Sign in" />;
};
export default Signin;
